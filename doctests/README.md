# Doctests

This module contains short executable documentation examples for the public `xjose` modules.

The examples exercise only public APIs, avoid external services and file-system dependencies, and validate stable
semantic output rather than randomized serialized JOSE values.

Run all doctests from the repository root:

```shell
go test ./doctests
```
