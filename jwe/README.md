# xjwt/jwe

Optional compact JWE and nested JWT support for `xjwt`, backed by `go-jose/v4`.

The module performs no network or file I/O. Applications own key loading and remote key-management clients.
